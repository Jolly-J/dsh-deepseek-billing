import type { Context } from '@deepseek-ai/cordis';
/** Register the exact status endpoint and its request-local aggregation flow. */
export declare function registerBillingStatusRoute(ctx: Context): () => void;
